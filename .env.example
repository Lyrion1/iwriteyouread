# Stripe Configuration for Donation Button

# Stripe Publishable Key (client-side, safe to expose)
# Get this from: Stripe Dashboard > Developers > API keys
# Set in Netlify: Site settings > Environment variables > STRIPE_PUBLISHABLE_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Secret Key (server-side only, NEVER commit to repository)
# Get this from: Stripe Dashboard > Developers > API keys
# Set in Netlify: Site settings > Environment variables > STRIPE_SECRET_KEY
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Legacy Stripe Checkout URL (optional, kept for backwards compatibility)
STRIPE_CHECKOUT_URL=https://your-real-stripe-link.com
